Following installations are required for the `sbibm` sbi-tasks:

install additional packages with `pip`:
- sbibm
- jax, numpyro (for generating reference posterior samples)
- julia + diffeqtorch (for sir and lotka volterra)
